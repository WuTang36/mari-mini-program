# Mari - 测试报告

> v1.0.0 MVP | 测试周期：2026-08-28 ~ 2026-08-31
> 关联文档：`需求文档.md` / `测试用例模板.md` / `Bug汇总.md`

---

## 一、测试概述

| 项目 | 内容 |
|------|------|
| 被测产品 | Mari（宠物健康管家微信小程序） |
| 版本 | v1.0.0 MVP |
| 测试范围 | 宠物档案、疫苗本、驱虫记录、体重记录、病史备注、血统信息、健康提醒、成长相册/日记、我的/登录、意见反馈 |
| 测试环境 | 微信开发者工具（macOS）+ 云开发环境 `cloud1-d7gq5krzxedfe3677` |
| 基础库 | 3.17.2（Skyline 渲染） |
| 测试方式 | 功能迭代测试 + 调试器报错排查 |

---

## 二、测试统计

| 指标 | 数值 |
|------|------|
| 发现缺陷总数 | 70 |
| 已修复 | 70 |
| 待处理（需部署/建集合后生效） | 0（部署类事项见「遗留」） |
| 缺陷关闭率 | 100% |

### 按严重程度分布

| 严重程度 | 数量 |
|---------|------|
| Critical | 17 |
| Major | 28 |
| Minor | 25 |
| Blocker | 0 |

### 按模块分布

| 模块 | 缺陷数 | 状态 |
|------|--------|------|
| 宠物档案 | 28 | ✅ 全部修复 |
| 疫苗本 | 4 | ✅ 全部修复 |
| 病史备注 | 4 | ✅ 全部修复 |
| 血统信息 | 4 | ✅ 全部修复 |
| 成长日记/相册 | 15 | ✅ 全部修复 |
| 健康提醒 | 6 | ✅ 全部修复 |
| 我的/登录/个人资料 | 6 | ✅ 全部修复 |
| 通用/编译环境 | 3 | ✅ 全部修复 |

---

## 三、各模块测试结论

### 3.1 宠物档案 ✅
- 登录：云函数 `login` 获取 openid，已接入云开发（非 mock）
- 添加/编辑/左滑删除：云数据库 CRUD 正常
- 品种：大类切换 + 子类选择 + 自定义品种弹窗
- 生日：日期选择默认今天、「生日未知」「估算年龄」均可用
- 性别图标与文字水平对齐

### 3.2 疫苗本 ✅
- 新增/列表展示正常，保存后列表即时刷新
- 到期日自动计算、照片上传 + 列表缩略图预览
- 保存后弹出订阅引导（可订阅/跳过）

### 3.3 驱虫记录 ✅
- 体内/体外分类展示、新增记录写库正常、日期默认今天

### 3.4 体重记录 ✅
- 体重保存持久化、日期选择默认今天、体重曲线展示

### 3.5 病史备注 ✅
- 保存写库、宠物卡片显示真实头像、附件照片缩略图 + 预览

### 3.6 血统信息 ✅
- 必填校验（证书编号/父/母品种）、证书照片上传、独立预览页（导航栏 + 卡片展示）

### 3.7 健康提醒 ✅
- 订阅消息：`sendReminder` 云函数定时推送（疫苗/驱虫前 7 天+当天；生日前 3 天+当天），发送去重
- 已读状态持久化、全部已读按钮常驻
- 订阅/取消订阅横幅按状态切换（`subscriptions` 集合）

### 3.8 成长相册/日记 ✅
- 宠物必选（写日记顶部 chips，保存校验）
- 类型筛选（照片/视频/日记）映射正确、切换宠物重置筛选
- 最新日记置顶（date + createdAt 排序）、分页滚动加载、固定头部与 FAB
- 照片预览、视频 `getTempFileURL` + `previewMedia` 播放、视频首帧封面
- 视频时长限制 15s

### 3.9 我的 / 登录 / 个人资料 ✅
- 登录按钮、随机昵称（openid 绑定唯一）、微信虚拟头像
- 个人信息编辑（chooseAvatar + nickname）、上下两行布局
- 关于我们弹窗（Mari + Logo + 微信号 @hoshinochiri）
- 意见反馈：提交 + 我的反馈记录（只读列表）

### 3.10 通用 ✅
- `wxfile://` 临时路径已统一清洗（数据层 `sanitizePetImages`）
- 图片加载失败降级回退占位
- 数据容错（`groupByMonth` 跳过非法日期）

---

## 四、遗留风险与注意事项

| 项 | 说明 | 处理 |
|----|------|------|
| 云函数部署 | `login`/`sendReminder`/`submitFeedback`/`getMyFeedback` 需部署 | 见《发布流程清单》 |
| 云数据库集合 | 需创建 7 个集合并设权限 | 见《发布流程清单》 |
| 订阅消息 | 体验版/开发版推送需 `MINIPROGRAM_STATE`；正式发布改 `formal` | 见《发布流程清单》 |
| Skyline WXS 报错 | 框架内部错误已规避（视频预览区分类型），若复现建议升级工具/基础库 | 低风险 |
| HTTP2 加载失败 | 开发者工具网络层偶发，已加图片降级 | 低风险 |

---

## 五、结论

✅ **测试通过，可进入发布准备阶段。**

- 70 个缺陷全部修复并验证
- 核心功能（宠物档案、疫苗、驱虫、体重、病史、血统、提醒、相册、登录、反馈）链路完整
- 剩余事项均为**部署/环境配置**类，不涉及代码缺陷，按《发布流程清单》执行即可
