# Mari - 项目备注

> 版本：v1.0.0 (MVP)
> 最后更新：2026-09-01
> 本文为项目的速览与备忘文档，供开发/维护/交接时快速查阅。详细设计见文末「关联文档」。

---

## 一、项目概述

| 项目 | 内容 |
|------|------|
| 项目名称 | **Mari**（宠物健康管家微信小程序） |
| 产品定位 | 面向养宠人群的宠物健康管理小程序，记录宠物档案、疫苗、驱虫、体重、病史、血统、成长相册与健康提醒 |
| 形态 | 微信小程序（原生） |
| 版本 | v1.0.0 MVP（`pages/mine/mine.wxml` 中展示「Mari v1.0.0」） |
| AppID | `wxa9eaf5f7f7b932b3` |
| 云开发环境 | `cloud1-d7gq5krzxedfe3677` |
| 开发工具 | 微信开发者工具（macOS），基础库 3.17.2 |
| 状态 | 功能开发完成，70 个缺陷全部修复（关闭率 100%），处于发布/内测阶段 |

---

## 二、技术栈

| 维度 | 选型 | 备注 |
|------|------|------|
| 渲染引擎 | **Skyline**（`app.json` 中 `renderer: "skyline"`） | 开发中曾因 tabBar 图标兼容问题切换过 WebView，最终回归 Skyline，见「重要备忘 1」 |
| 组件框架 | Glass-Easel（`componentFramework: "glass-easel"`） | 页面多采用 `Component()` + `pageLifetimes` 写法 |
| 后端 | 微信云开发（CloudBase） | 云函数 + 云数据库 + 云存储 |
| 导航栏 | 自定义 `components/navigation-bar/` 组件 | 全局 `navigationStyle: "custom"`，统一状态栏/标题 |
| tabBar | 原生 tabBar（3 个 Tab） | `custom-tab-bar.bak/` 为废弃的自定义 tabBar 备份 |
| 样式 | CSS 变量 + 类 BEM 命名 | 主题色集中在 `app.wxss` |

---

## 三、目录结构

```
mini-program/
├── app.js / app.json / app.wxss     # 应用入口、全局配置、全局样式
├── project.config.json              # AppID / cloudfunctionRoot 等
├── sitemap.json                     # 搜索索引配置
├── Logo.png                         # 品牌 Logo（关于弹窗、头像兜底）
├── components/
│   └── navigation-bar/              # 自定义导航栏组件
├── pages/                           # 20 个页面
│   ├── index/                       # Tab1 宠物首页（提醒卡片 + 宠物列表）
│   ├── pet-detail/                  # 宠物详情（健康管理入口）
│   ├── pet-form/                    # 添加/编辑宠物
│   ├── vaccine-list/ vaccine-form/  # 疫苗本列表 / 新增疫苗
│   ├── deworming-list/ deworming-form/  # 驱虫记录（体内/体外）
│   ├── weight-list/ weight-form/    # 体重曲线 / 记录体重
│   ├── medical-list/ medical-form/  # 病史时间线 / 新增病史
│   ├── pedigree/ pedigree-preview/  # 血统信息 / 血统预览
│   ├── album/                       # Tab2 成长相册（时间线 + 筛选）
│   ├── diary-form/                  # 写日记
│   ├── mine/                        # Tab3 我的
│   ├── feedback/ my-feedback/       # 意见反馈 / 我的反馈
│   ├── reminder-center/             # 提醒中心（订阅/已读）
│   └── profile-edit/                # 个人资料编辑
├── cloudfunctions/                  # 5 个云函数
│   ├── login/                       # 获取 openid
│   ├── sendReminder/                # 订阅消息定时推送（含定时触发器）
│   ├── submitFeedback/              # 意见反馈写入
│   ├── getMyFeedback/               # 我的反馈查询
│   └── deletePetData/               # 删除宠物（含日记/归档逻辑）
├── utils/
│   ├── cloud.js                     # 云函数调用封装（统一日志）
│   ├── mock.js                      # Mock 数据（早期开发用）
│   ├── storage.js                   # 本地缓存封装
│   └── util.js                      # 日期/年龄/分组等工具
├── images/                          # tabBar 图标等静态资源
├── ui-designs/                      # 16 个页面 UI 设计稿（HTML）
├── tabbar-designs/                  # tabBar 图标设计源文件
└── 文档：需求文档 / 技术方案 / 测试报告 / Bug汇总 / 发布流程清单 / 测试用例模板 / Bug报告模板
```

---

## 四、功能模块

| 模块 | 说明 | 状态 |
|------|------|------|
| 宠物电子档案 | 多宠物管理、左滑删除、头像上传/裁剪、品种 AI 识别（云函数）、生日/估算年龄 | ✅ |
| 疫苗本 | 记录名称/接种日期/到期日/批号/机构/照片，自动关联提醒 | ✅ |
| 驱虫记录 | 体内/体外独立列表，用药与下次日期 | ✅ |
| 体重记录 | 记录列表 + 自动生成变化曲线（kg/斤切换） | ✅ |
| 病史备注 | 时间线展示，标签分类（手术/慢性病/过敏/受伤/其他）+ 附件照片 | ✅ |
| 血统信息 | 可折叠填写，证书编号/父母品种/繁育者 + 证书照片 | ✅ |
| 健康提醒 | 疫苗/驱虫/生日到期提醒，订阅消息推送（一次性订阅），提醒中心一键订阅、已读管理、首页角标 | ✅ |
| 成长相册/日记 | 按宠物独立时间线、按月分组、照片/视频/文字筛选、里程碑标签 | ✅ |
| 我的/登录 | 微信授权登录、头像昵称、个人资料编辑 | ✅ |
| 意见反馈 | 提交反馈 + 我的反馈记录 | ✅ |

---

## 五、云开发资源

### 5.1 云数据库集合（权限：仅创建者可读写）

| 集合 | 用途 |
|------|------|
| `pets` | 宠物档案 |
| `users` | 用户资料 |
| `diaries` | 成长日记 |
| `feedbacks` | 意见反馈 |
| `reminder_reads` | 提醒已读状态 |
| `reminder_sends` | 订阅消息发送去重 |
| `subscriptions` | 订阅状态 |
| `deleted_pets` | 删除宠物归档（`deletePetData` 使用，相册 Tab 保留已归档宠物的日记） |

### 5.2 订阅消息模板

| 类型 | 模板 ID | 关键字 |
|------|---------|--------|
| 到期提醒 | `DG-B5rqPc65CeE8eo0JjcdNrQd90dc9wakSo5auDZ7U` | 复查时间、剩余天数、备注 |
| 生日提醒 | `n2U04kHIkdjq_vz4fxYnmqnEVOx6xVAD37debwkw6Uk` | 日程时间、温馨提示 |

> ⚠️ 小程序端 `pages/reminder-center/reminder-center.js` 与云函数 `sendReminder` 中的模板 ID 必须保持一致。

---

## 六、重要备忘（易踩坑 / 历史决策）

1. **渲染引擎的反复**：早期曾因 Skyline 下原生 tabBar 图标渲染异常（`<image>` 在 `defaultDisplayBlock: true` 下行为异常）而整体切换 WebView；后续已解决并回归 **Skyline + Glass-Easel**。`技术方案.md` 中「放弃 Skyline」一节为历史记录，以当前 `app.json` 配置为准。
2. **数据演进**：`技术方案.md` 中「MVP 使用 Mock 数据」为早期方案；当前已全面接入云开发（登录、宠物 CRUD、反馈等），`utils/mock.js` 仅保留作兜底/参考。
3. **云函数部署方式**：需在开发者工具中对每个云函数目录**右键 → 上传并部署：云端安装依赖（不上传 node_modules）**。未部署或集合未创建会报 `-501000` / `-502005` 等错误。
4. **订阅消息限制**：微信订阅消息为「一次性订阅」，每次订阅仅触发一次推送，故提醒中心采用「集中展示未来 7 天待提醒 + 一键订阅」策略；一次调用最多订阅 3 个模板。
5. **`sendReminder` 发布前必改**：`MINIPROGRAM_STATE` 由 `'developer'` 改为 `'formal'` 后重新部署；该云函数含每天 8:00 的定时触发器与 `subscribeMessage.send` 权限。
6. **删除宠物是「归档 + 删除」**：`deletePetData` 默认先归档到 `deleted_pets` 再删宠物（相册保留归档宠物的日记），归档失败则中止删除，避免「删了宠物但 Tab 丢失」；仅 `petAndDiaries` 模式才连日记一起物理删除。
7. **自定义导航栏**：全局 `navigationStyle: "custom"`，新增页面必须使用 `components/navigation-bar/`，否则状态栏/标题会出现错位。
8. **`custom-tab-bar.bak/` 为废弃备份**：当前使用原生 tabBar，勿将其恢复为正式组件目录。
9. **git 状态**：当前仓库所有文件尚未提交（首次提交待做），无 README/`.gitignore`，`../../.DS_Store` 等文件注意不要误提交。

---

## 七、发布前待办（详见 `发布流程清单.md`）

- [ ] 全局名称统一为 **Mari**（无「宠物健康管家」残留）
- [ ] 部署 5 个云函数（login 已部署可跳过，submitFeedback / getMyFeedback / deletePetData 需确认）
- [ ] 云开发控制台创建全部集合并设置权限
- [ ] 小程序后台确认订阅消息模板 ID 一致
- [ ] `sendReminder` 的 `MINIPROGRAM_STATE` 改为 `formal` 后重新部署
- [ ] 上传体验版 → 体验成员验证 → 提审 → 发布 → 按清单做发布后回归

---

## 八、关联文档

| 文档 | 说明 |
|------|------|
| `需求文档.md` | 产品需求与功能规格（v1.0.0 MVP） |
| `技术方案.md` | 技术选型与架构（部分内容为历史方案，见「重要备忘 1/2」） |
| `测试报告.md` | v1.0.0 测试结论（70 缺陷全部修复） |
| `Bug汇总.md` | 缺陷汇总表（BUG-001 起） |
| `发布流程清单.md` | 发布前检查、部署步骤、发布后验证、回滚预案 |
| `测试用例模板.md` / `Bug报告模板.md` | 测试与缺陷报告规范 |
