# Mari - 发布流程清单

> v1.0.0 MVP | 适用于体验版内测 → 正式版发布
> 关联文档：`需求文档.md` / `测试报告.md` / `Bug汇总.md`

---

## 一、发布前代码检查

- [ ] 全局名称统一为 **Mari**（无「宠物健康管家」残留）
- [ ] `app.js` 云环境 ID 为 `cloud1-d7gq5krzxedfe3677`
- [ ] `app.json` 包含 `cloud: true`、`renderer: skyline`、`rendererOptions.skyline`、`componentFramework: glass-easel`
- [ ] `project.config.json` 已配置 `cloudfunctionRoot: "cloudfunctions/"`
- [ ] 版本号：`pages/mine/mine.wxml` 版本文案为 `Mari v1.0.0`
- [ ] `Logo.png` 已置于项目根目录并被引用（关于我们弹窗、宠物头像兜底）

---

## 二、云函数部署（微信开发者工具）

对以下目录**逐个右键 → 上传并部署：云端安装依赖（不上传 node_modules）**：

| 云函数 | 作用 | 说明 |
|--------|------|------|
| `login` | 获取 openid | 已部署则跳过 |
| `sendReminder` | 订阅消息推送 | 含定时触发器（每天 8:00）与 `subscribeMessage.send` 权限 |
| `submitFeedback` | 意见反馈写入 | 需新部署 |
| `getMyFeedback` | 我的反馈查询 | 需新部署 |

> 部署后建议在云开发控制台「云端测试」各运行一次，确认返回 `success: true`。

---

## 三、云数据库集合（云开发控制台 → 数据库）

创建以下集合并设置权限「仅创建者可读写」：

| 集合 | 用途 |
|------|------|
| `pets` | 宠物档案 |
| `users` | 用户资料 |
| `diaries` | 成长日记 |
| `feedbacks` | 意见反馈 |
| `reminder_reads` | 提醒已读状态 |
| `reminder_sends` | 订阅消息发送去重 |
| `subscriptions` | 订阅状态 |

---

## 四、订阅消息配置（小程序后台）

- [ ] 后台「功能 → 订阅消息」已添加两个模板：
  - 到期提醒：`DG-B5rqPc65CeE8eo0JjcdNrQd90dc9wakSo5auDZ7U`（关键字：复查时间、剩余天数、备注）
  - 生日提醒：`n2U04kHIkdjq_vz4fxYnmqnEVOx6xVAD37debwkw6Uk`（关键字：日程时间、温馨提示）
- [ ] 小程序端 `pages/reminder-center/reminder-center.js` 与云函数 `sendReminder` 中的模板 ID 一致

---

## 五、正式版配置切换

- [ ] 云函数 `sendReminder/index.js`：`MINIPROGRAM_STATE` 由 `'developer'` 改为 **`'formal'`** 后重新部署
- [ ] （可选）`project.config.json` 基础库由 `trial` 改为稳定版本

---

## 六、发布步骤

### 6.1 体验版（内测）
1. 开发者工具 → 右上角「上传」→ 填版本号 `1.0.0` 与备注 → 上传
2. 小程序后台 →「版本管理」→ 将上传版本设为**体验版**
3. 添加体验成员，扫码验证核心流程

### 6.2 正式版（提审）
1. 体验版验证通过后 →「提交审核」
2. 审核说明建议：
   - 简述：宠物健康管理小程序，记录宠物档案、疫苗、驱虫、体重、病史、血统、成长相册与提醒
   - 登录说明：使用微信授权登录（`getUserProfile`），头像昵称用于个人主页展示
   - 订阅消息：提醒疫苗/驱虫/生日到期（一次性订阅）
3. 审核通过后 →「发布」

---

## 七、发布后验证清单

- [ ] 冷启动：控制台输出「云开发就绪，openid: xxx」
- [ ] 登录：拉起微信授权，头像/昵称正确，随机昵称「铲屎官_xxxx」不变
- [ ] 添加/编辑/删除宠物、上传头像（云存储 `cloud://`）
- [ ] 疫苗/驱虫/体重/病史/血统新增后列表即时刷新
- [ ] 写日记：宠物必选、上传照片/视频（≤15s）、相册展示与预览
- [ ] 提醒：订阅弹窗正常，`sendReminder` 定时运行，订阅/取消横幅切换
- [ ] 意见反馈：提交 + 我的反馈记录
- [ ] 无 `wxfile://` ENOENT、无集合缺失报错
- [ ] 真机扫体验码回归一遍核心链路

---

## 八、紧急回滚预案

- 若正式版出现问题：小程序后台「版本管理 → 全量回退」到上一稳定版本
- 云函数问题：云开发控制台可回退云函数历史版本
- 数据误删：云数据库控制台可恢复最近备份（建议发布前开启备份）
