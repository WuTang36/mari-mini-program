# 宠物健康管家 — 技术方案

> **版本**：v1.0.0 (MVP)
> **日期**：2026-08-28
> **AppID**：wxa9eaf5f7f7b932b3

---

## 一、技术架构总览

### 1.1 整体架构

```
┌─────────────────────────────────────────┐
│               微信小程序                  │
│  ┌─────────────────────────────────┐    │
│  │         View Layer (WXML)       │    │
│  │    Component() + pageLifetimes  │    │
│  └──────────────┬──────────────────┘    │
│  ┌──────────────┴──────────────────┐    │
│  │       Business Logic (JS)       │    │
│  │    utils/mock.js → utils/util.js │    │
│  └──────────────┬──────────────────┘    │
│  ┌──────────────┴──────────────────┐    │
│  │    微信云开发 (CloudBase)        │    │
│  │  ├─ 云数据库 (集合 7 个)         │    │
│  │  ├─ 云存储 (图片/视频)           │    │
│  │  └─ 云函数 (品种识别/提醒推送)    │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

### 1.2 技术选型

| 维度 | 选择 | 原因 |
|------|------|------|
| 渲染引擎 | **WebView**（非 Skyline） | Skyline 与 tabBar 存在兼容性问题，原生 tabBar 在 Skyline 下无法渲染图标 |
| 页面模式 | `Component()` + `pageLifetimes` | 便于复用生命周期逻辑，兼容 WebView |
| 导航栏 | 自定义 `navigation-bar` 组件 | 全局 `navigationStyle: "custom"`，统一 UI 风格 |
| 后端 | 微信云开发 | 免运维、免域名、天然鉴权 |
| 数据 | MVP 阶段 Mock 数据 | 前端独立开发，后续切换到云数据库 |
| 样式 | CSS 变量 + BEM 命名 | 统一主题色、间距、圆角 |

### 1.3 关键架构决策

**决策 1：放弃 Skyline，使用 WebView**

Skyline 渲染引擎对原生 tabBar 的图标渲染存在兼容性问题，`<image>` 在 Skyline 的 `defaultDisplayBlock: true` 下行为异常，导致图标无法正常显示。全局切换 WebView 后问题解决。后续若微信修复此问题，可重新评估迁移。

**决策 2：MVP 阶段使用 Mock 数据，不接入云开发**

当前所有页面使用 `utils/mock.js` 中的静态数据，独立于云开发运行。后端接入作为后续迭代任务，MVP 聚焦前端功能完整性和 UI 体验。

**决策 3：原生 tabBar + 自定义导航栏**

- 原生 tabBar：3 个 tab（宠物/相册/我的），支持图标和文字
- 自定义导航栏：`components/navigation-bar/` 统一处理状态栏高度、返回按钮、标题

---

## 二、项目结构

```
mini-program/
├── app.js                    # 应用入口，wx.login
├── app.json                  # 全局配置（tabBar/pages/window）
├── app.wxss                  # 全局样式（CSS 变量）
├── project.config.json       # 项目配置（AppID/libVersion）
├── sitemap.json              # 搜索索引配置
│
├── components/
│   └── navigation-bar/       # 自定义导航栏组件
│       ├── navigation-bar.js
│       ├── navigation-bar.json
│       ├── navigation-bar.wxml
│       └── navigation-bar.wxss
│
├── pages/
│   ├── index/                # Tab 1 - 宠物首页（提醒卡片 + 宠物列表）
│   ├── pet-detail/           # 宠物详情（健康管理入口 + 快捷操作）
│   ├── pet-form/             # 添加/编辑宠物（头像/品种/性别/生日/体重）
│   ├── vaccine-list/         # 疫苗本列表
│   ├── vaccine-form/         # 添加/编辑疫苗记录
│   ├── deworming-list/       # 驱虫记录列表（体内/体外）
│   ├── deworming-form/       # 添加/编辑驱虫记录
│   ├── weight-list/          # 体重曲线 + 记录列表
│   ├── weight-form/          # 记录体重
│   ├── medical-list/         # 病史时间线
│   ├── medical-form/         # 添加/编辑病史
│   ├── pedigree/             # 血统信息
│   ├── album/                # Tab 2 - 成长相册（时间线 + 筛选）
│   ├── diary-form/           # 写日记（媒体/标签/日期）
│   ├── mine/                 # Tab 3 - 我的（用户信息/菜单）
│   └── reminder-center/      # 提醒中心（订阅/已读/划线）
│
├── utils/
│   ├── mock.js               # Mock 数据（宠物/提醒/日记）
│   └── util.js               # 工具函数（日期/年龄/分组）
│
├── images/                   # 图标资源
│   ├── tab-pet.png           # 宠物 tab 图标（81×81）
│   ├── tab-pet-active.png
│   ├── tab-album.png         # 相册 tab 图标（81×81）
│   ├── tab-album-active.png
│   ├── tab-mine.png          # 我的 tab 图标（81×81）
│   └── tab-mine-active.png
│
├── 需求文档.md               # 产品需求文档
└── 技术方案.md               # 本文档
```

---

## 三、数据流设计

### 3.1 当前状态（Mock 数据）

```
┌──────────────┐      require()       ┌──────────────────┐
│  utils/mock.js│ ──────────────────→  │  pages/*/xxx.js  │
│  (静态数据)    │                      │  (Component data) │
└──────────────┘                      └────────┬─────────┘
                                               │ setData()
                                               ▼
                                        ┌──────────────┐
                                        │  WXML 渲染    │
                                        └──────────────┘
```

### 3.2 目标状态（云开发接入后）

```
┌──────────────┐   wx.cloud.database()   ┌──────────────┐
│  云数据库     │ ◄────────────────────── │  pages/*.js  │
│  (7 集合)    │                         │              │
└──────┬───────┘                         └──────┬───────┘
       │                                        │
       │  wx.cloud.uploadFile()                 │ setData()
       ▼                                        ▼
┌──────────────┐                         ┌──────────────┐
│  云存储       │                         │  WXML 渲染    │
│  (图片/视频)  │                         └──────────────┘
└──────────────┘
```

### 3.3 数据模型（云数据库集合）

```
pets
├── _id, _openid
├── name, avatar, breed, gender, birthday
├── isEstimatedAge: boolean
└── pedigreeInfo: { certNo, fatherBreed, motherBreed, breeder, certPhoto }

weight_records
├── _id, _openid, petId
├── date, weight, unit

vaccine_records
├── _id, _openid, petId
├── name, date, nextDate, batchNo, clinic, photo

deworming_records
├── _id, _openid, petId
├── type(体内/体外), medicine, date, nextDate

medical_records
├── _id, _openid, petId
├── date, title, tag(手术/慢性病/过敏/受伤/其他), description, photos

diaries
├── _id, _openid, petId
├── date, content, media[], milestoneTags[]
├── createdAt, updatedAt

reminders
├── _id, _openid, petId, type
├── dueDate, remindBeforeDays, isRead, relatedRecordId
```

---

## 四、页面技术实现

### 4.1 页面清单与状态

| 页面 | 文件 | 状态 | 说明 |
|------|------|:----:|------|
| 宠物首页 | `pages/index/` | ✅ | 提醒卡片 + 宠物列表 + 引导横幅 |
| 宠物详情 | `pages/pet-detail/` | ✅ | 名片 + 健康管理入口 + 快捷操作 |
| 宠物表单 | `pages/pet-form/` | ✅ | 头像/品种面板/日期选择器/性别切换 |
| 疫苗列表 | `pages/vaccine-list/` | ✅ | 按时间倒序，关联提醒 |
| 疫苗表单 | `pages/vaccine-form/` | ✅ | 自动计算下次到期 |
| 驱虫列表 | `pages/deworming-list/` | ✅ | 体内/体外分开 Tab |
| 驱虫表单 | `pages/deworming-form/` | ✅ | 悬浮按钮选择体内/体外 |
| 体重曲线 | `pages/weight-list/` | ⚠️ | 折线图待实现（目前占位符） |
| 体重表单 | `pages/weight-form/` | ✅ | 数字输入，限制两位小数 |
| 病史列表 | `pages/medical-list/` | ✅ | 时间线展示 |
| 病史表单 | `pages/medical-form/` | ✅ | 标签分类 + 附件 |
| 血统信息 | `pages/pedigree/` | ✅ | 可折叠 + 提示条 |
| 成长相册 | `pages/album/` | ✅ | 宠物选择器 + 时间线 + 筛选 |
| 写日记 | `pages/diary-form/` | ✅ | 媒体/标签/日期 |
| 我的 | `pages/mine/` | ✅ | 用户卡片 + 菜单 |
| 提醒中心 | `pages/reminder-center/` | ✅ | 订阅横幅 + 已读/未读 + 划线 |

### 4.2 关键技术点

#### 4.2.1 品种选择面板（pet-form）

- 底部弹出面板（`sheet-overlay` + `sheet-panel`）
- 3 个 Tab：🐱 猫 / 🐶 狗 / 🐹 其他
- 每个 Tab 对应预设品种列表（10-11 个）
- 点击选中后自动关闭面板

#### 4.2.2 日期选择器（pet-form / diary-form）

- 使用 `<picker-view>` 三列滚动（年/月/日）
- 底层覆盖半透明遮罩，点击遮罩关闭
- "生日不记得" 工具行：🤷 我不知道 / 📅 估算年龄

#### 4.2.3 体重折线图（weight-list）

- 当前为占位符，后续实现方案：
- 使用 Canvas 2D API 绘制折线图
- Tab 切换（近1月/近3月/近半年/近1年/全部）对应横坐标范围
- 点击折点弹出数据详情

#### 4.2.4 成长相册时间线（album）

- 左侧时间轴（圆点 + 竖线）+ 右侧卡片
- 按月分组（`groupByMonth` 工具函数）
- 筛选器：全部 / 照片 / 视频 / 日记
- 横向宠物选择器使用 `overflow-x: auto`（避免 Skyline 嵌套 scroll-view 冲突）

#### 4.2.5 提醒中心（reminder-center）

- 订阅横幅（顶部）
- "全部已读" 按钮（导航栏下方）
- 已读提醒：文字划线（`strikethrough`）+ 蓝色 ✓ 标记
- 未读提醒：红色圆点标记
- 点击切换已读/未读状态

#### 4.2.6 自定义导航栏（navigation-bar）

- 使用 `wx.getMenuButtonBoundingClientRect()` 获取胶囊按钮位置
- 自动适配 iOS/Android 状态栏高度
- 支持返回按钮、标题、右侧插槽
- 首页 tab 不显示返回按钮（`back="{{false}}"`）

---

## 五、样式系统

### 5.1 CSS 变量（app.wxss）

```css
--color-primary: #FF9800;        /* 主色调：暖橙 */
--color-primary-light: #FFB74D;  /* 浅橙 */
--color-bg: #F8F6F2;             /* 背景色：暖灰 */
--color-card: #FFFFFF;           /* 卡片色 */
--color-text: #2D2D2D;           /* 主文字 */
--color-text-secondary: #555555;
--color-text-hint: #999999;
--color-text-placeholder: #C8B8A0;
--color-border: #F5F2ED;
--color-divider: #E8E4DC;
--color-danger: #FF5252;
--color-success: #4CAF50;
```

### 5.2 通用组件类

- `.btn-primary`：主按钮（渐变背景 + 阴影 + 缩放反馈）
- `.form-row`：表单行（标签 + 值 + 箭头）
- `.card`：通用卡片（圆角 + 阴影）
- `.sheet-overlay` / `.sheet-panel`：底部弹出面板
- `.empty-state`：空状态占位

---

## 六、已知问题与待解决

### 6.1 当前阻塞

| 问题 | 优先级 | 状态 |
|------|:------:|:----:|
| **tabBar 图标不显示** | 🔴 P0 | 根因：图标文件内容为空白。已用 PIL 重新生成 81×81 PNG。若仍不显示，建议改用纯文字 tabBar（删除 iconPath/selectedIconPath） |
| 体重折线图未实现 | 🟡 P1 | 当前为占位符，需 Canvas 2D 实现 |
| 页面卡片溢出 | 🟡 P1 | 部分页面 `scroll-view` padding 导致子元素溢出，已修复核心页面 |

### 6.2 tabBar 图标终极方案

若重新生成的图标仍不显示，采用以下备用方案：

**方案 A：纯文字 tabBar**
```json
"tabBar": {
  "list": [
    { "pagePath": "pages/index/index", "text": "🐾 宠物" },
    { "pagePath": "pages/album/album", "text": "📸 相册" },
    { "pagePath": "pages/mine/mine", "text": "👤 我的" }
  ]
}
```
删除 `iconPath` 和 `selectedIconPath`，仅靠文字区分 tab。

**方案 B：自定义 tabBar 组件 + WebView 渲染**
恢复 `custom-tab-bar/` 组件，但确保 tab 页面使用 WebView 渲染（非 Skyline）。WebView 下自定义 tabBar 可正常工作。

### 6.3 其他待优化

- 图片懒加载
- 视频压缩上传
- 离线缓存策略
- 微信订阅消息模板申请
- 云函数开发（品种识别、提醒推送）

---

## 七、部署与测试

### 7.1 开发环境

- 微信开发者工具（稳定版）
- 基础库版本：3.0+（`libVersion: "trial"`）
- 测试设备：iPhone 15 Pro Max 模拟器

### 7.2 测试要点

| 模块 | 测试场景 |
|------|---------|
| 宠物管理 | 添加/编辑/删除宠物、品种选择面板、日期选择器、头像上传 |
| 健康管理 | 疫苗/驱虫增删改查、体重记录、病史时间线 |
| 提醒 | 提醒列表、已读/未读切换、全部已读、订阅引导 |
| 相册 | 时间线分组、筛选器切换、宠物选择器横滑、日记编辑 |
| 通用 | 页面跳转、tab 切换、空状态、导航栏返回 |

### 7.3 发布流程

1. 代码审核：检查所有页面功能完整性
2. 云开发配置：初始化云环境、创建数据库集合、部署云函数
3. 替换 Mock 数据：将 `utils/mock.js` 引用替换为云数据库调用
4. 上传代码：微信开发者工具 → 上传
5. 提交审核：微信公众平台 → 版本管理 → 提交审核

---

## 八、后续迭代规划

| 迭代 | 内容 |
|:----:|------|
| V1.1 | 云开发接入（数据库 + 存储 + 云函数） |
| V1.2 | 体检/绝育/用药/洗澡提醒 |
| V1.3 | 体重折线图（Canvas 2D） |
| V1.4 | 品种 AI 识别云函数 |
| V1.5 | 成长相册分享（拼图/朋友圈） |
| V2.0 | 血统树图谱、多宠主共享、宠物社交 |
